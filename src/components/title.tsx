const Title = () => {
  const title = "Hello world, this is first video.";
  return (
    <div>
      <div className="rounded-md border px-4 py-2 font-mono text-sm shadow-sm">
        {title}
      </div>
    </div>
  );
};

export default Title;
